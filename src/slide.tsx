import styled from '@emotion/styled';

const Slide = styled.div`
  width: 100%;
  height: 100%;
  will-change: transform;
  user-select: none;
  pointer-events: none;
  margin: auto 0;
`;

export default Slide;
